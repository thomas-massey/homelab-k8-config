apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nginx-hello-cert
spec:
  secretName: nginx-hello-tls
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
    - nginx.example.com
