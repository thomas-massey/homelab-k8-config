apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: amp-data
  namespace: amp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: amp-data-pv
  resources:
    requests:
      storage: 50Gi
